# Elevvo Internship - Task 8

## Data Requirements
- **Dataset:** GTSRB (German Traffic Sign Recognition Benchmark)
- **Expected image folder:** `GTSRB/gtsrb/gtsrb/` with class folders `0` to `42`.
- **Notebook expectation:** class image files are organized by class-id directories.

## Repository Data Policy
- Do not commit full image datasets unless explicitly required.
- Keep full GTSRB dataset local and commit only notebook/code/results.
- Document dataset source and folder layout for reproducibility.

## Quick Setup
1. Download/extract GTSRB so class folders are under `Machine Learning Track/GTSRB/gtsrb/gtsrb/`.
2. Confirm class directories (`0` ... `42`) contain image files.
3. Run `Task_8_Solution.ipynb`.
